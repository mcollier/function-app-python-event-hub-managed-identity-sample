# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: function-app-python-event-hub-managed-identity-sample
infra:
  provider: bicep
  path: infra
services:
  myapp:
    project: src
    language: python
    host: function